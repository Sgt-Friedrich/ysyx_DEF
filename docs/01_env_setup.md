# 环境准备

## 依赖检查方式
- git: `git --version`
- gcc: `gcc --version`
- g++: `g++ --version`
- make: `make --version`
- verilator(可选): `verilator --version`
- python(如需): `python3 --version`

## 答辩现场建议
- 提前运行：`scripts/env_check.sh`
- 运行演示前确认环境变量与路径正确
